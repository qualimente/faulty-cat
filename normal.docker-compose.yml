version: '2'

networks:
  faulty-cat:

services:
    server:
        build: server
        ports:
            - "4242"
        expose:
            - "4242"
        hostname: server
        networks:
            - faulty-cat
        pid: host

    client:
        build: client
        networks:
            - faulty-cat
